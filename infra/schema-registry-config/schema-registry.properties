# Schema Registry configuration
# Bitnami's env-var mapping for SCHEMA_REGISTRY_KAFKASTORE_BOOTSTRAP_SERVERS
# doesn't seem to work in the 7.6 image; mount the config file directly.
kafkastore.bootstrap.servers=PLAINTEXT://kafka:9094
listeners=http://0.0.0.0:8081
host.name=schema-registry
