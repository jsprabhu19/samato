# Samato Microservice Bible 🍔

A complete reference microservice system designed to be your **interview preparation bible** for backend / platform engineering roles.

> **Project:** Samato
> **Domain:** Food delivery (like Uber Eats / DoorDash)
> **Why this domain:** Exercises nearly every microservice concern in one realistic flow — gateways, sagas, async events, distributed transactions, caching, resilience, observability, security, and more.

---

## TL;DR

```
9 Spring Boot 3.3.4 services on Java 21 + Spring Cloud 2023.0.3
132 Java files, ~9,000 lines
PostgreSQL (per-service DBs) + Kafka + Redis + OpenSearch
Event-sourced payment ledger with Razorpay integration
Distributed saga for the order workflow
End-to-end JWT auth via Spring Authorization Server
Docker Compose for the full stack
INTERVIEW-NOTES per service — designer docs for the "why"
```

| What | Where |
|---|---|
| Operator guide (how to run) | [`RUN-THE-BIBLE.md`](RUN-THE-BIBLE.md) |
| Architecture (containers, ADRs) | [`ARCHITECTURE.md`](ARCHITECTURE.md) |
| Project status (what's done / pending) | [`PROJECT-STATUS.md`](PROJECT-STATUS.md) |
| Interview cheat-sheet (Q&A) | [`docs/INTERVIEW-CHEATSHEET.md`](docs/INTERVIEW-CHEATSHEET.md) |
| Service designer notes | `services/*/docs/INTERVIEW-NOTES.md` |

---

## Quick start

For the long version (prerequisites, troubleshooting, what to do when things break), see [`RUN-THE-BIBLE.md`](RUN-THE-BIBLE.md). The minimum:

```bash
# 1. Prerequisites: Java 21, Maven 3.9+, Docker Desktop
java -version && mvn -version && docker --version

# 2. Compile (expect a few small errors on first run; see RUN-THE-BIBLE.md §1)
mvn -B -DskipTests compile

# 3. Bring up the full stack
cd infra && docker compose up -d
# wait 60-90s for Spring contexts to start
```

Verify:
- Eureka dashboard: <http://localhost:8761>
- API Gateway:    <http://localhost:8080>
- Kafka UI:       <http://localhost:8081>
- Prometheus:     <http://localhost:9090>
- Grafana:        <http://localhost:3000> (admin / admin)
- Zipkin:         <http://localhost:9411>

The end-to-end happy path (register → login → place order → saga runs → payment attempts) is in [`RUN-THE-BIBLE.md` §4](RUN-THE-BIBLE.md).

---

## What's in this repo

### Services (9 total, all on Java 21 + Spring Boot 3.3.4)

| Service | Port | Pattern demonstrated |
|---|---:|---|
| **api-gateway** | 8080 | Spring Cloud Gateway, JWT validation, rate limit, route config |
| **config-service** | 8888 | Spring Cloud Config Server, git-backed config repo |
| **discovery-service** | 8761 | Netflix Eureka registry |
| **auth-service** | 9000 | Spring Authorization Server, JWT issuance, JWKS endpoint |
| **user-service** | 8081 | Profile management, role-based access |
| **restaurant-service** | 8082 | Restaurant + menu catalog, OpenSearch index |
| **search-service** | 8087 | CQRS read model, OpenSearch + Redis cache |
| **order-service** | 8083 | Saga orchestrator, outbox, idempotency, Feign clients |
| **payment-service** | 8084 | **Event-sourced** ledger, Razorpay integration, HMAC webhook |

> Note: AUTH port. `auth-service` runs on **9000** (Spring Authorization Server's default). The gateway validates JWTs by hitting `auth-service:9000/.well-known/jwks.json` — there is no Keycloak in this build.

### Infrastructure

| Component | Version | Purpose |
|---|---|---|
| PostgreSQL | 16 | One database per service (`payment_service`, `order_service`, …) |
| Apache Kafka | 3.7 | Async messaging, outbox fanout, schema-registry companion |
| Redis | 7 | Cache-aside, rate-limit tokens, distributed locks |
| OpenSearch | 2.x | Search index for restaurant catalog |
| Zipkin | latest | Distributed tracing UI |
| Prometheus | latest | Metrics scrape |
| Grafana | latest | Dashboards (admin/admin, datasource pre-provisioned) |

### Shared modules

- **`shared/`** — cross-cutting code: `DomainException`, `GlobalExceptionHandler`, `CorrelationIdFilter`, `MdcKeys`. Every service depends on this.
- **`shared-kafka/`** — Avro schemas for cross-service events. The Java classes are generated by the Avro Maven plugin. Three schemas today: `PaymentChargedEvent`, `PaymentRefundedEvent`, `PaymentFailedEvent`.

### Top-level docs

```
samato/
├── README.md                        # this file
├── ARCHITECTURE.md                  # C4 diagrams, ADRs, communication patterns
├── PROJECT-STATUS.md                # what's done, what's pending, what's risky
├── RUN-THE-BIBLE.md                 # operator's guide — how to run, troubleshoot
├── .gitignore
├── pom.xml                          # Maven multi-module parent
├── config-repo/                     # top-level shared config (loaded by config-service)
├── docs/
│   └── INTERVIEW-CHEATSHEET.md      # Q&A by topic
├── infra/                           # docker-compose, prometheus, grafana, postgres init
├── shared/                          # cross-cutting library
├── shared-kafka/                    # Avro schemas
└── services/
    ├── api-gateway/
    ├── auth-service/
    ├── config-service/
    ├── discovery-service/
    ├── order-service/
    ├── payment-service/
    ├── restaurant-service/
    ├── search-service/
    └── user-service/
```

Each `services/<name>/` follows the same shape:
```
services/payment-service/
├── pom.xml
├── Dockerfile
├── docs/INTERVIEW-NOTES.md          # service-specific design notes
└── src/
    ├── main/
    │   ├── java/com/samato/<svc>/   # package root
    │   └── resources/
    │       ├── application.yml
    │       └── db/migration/        # Flyway V1__, V2__, ...
    └── test/                        # (mostly empty — see PROJECT-STATUS.md)
```

---

## Phases

| Phase | Services | Status | Notable patterns |
|---|---|---|---|
| 1 | config, discovery, api-gateway | ✅ done | Eureka, Spring Cloud Config, gateway routing |
| 2 | auth-service, user-service | ✅ done | Spring Authorization Server, OAuth2/JWT, RBAC |
| 3 | restaurant-service, search-service | ✅ done | CQRS, OpenSearch, Redis cache |
| 4 | order-service | ✅ done | Saga orchestrator, transactional outbox, idempotency, Feign clients |
| 5 | payment-service | ✅ done | Event sourcing, Razorpay integration, HMAC webhook, snapshots, time-travel |
| 6 | delivery-service | ⏳ next | Driver assignment, WebSocket, geo queries |
| 7 | notification, analytics | ⏳ | Pub/Sub fanout, Kafka Streams |
| 8 | Hardening | ⏳ | Testcontainers, Pact, DLQs, ADRs, k6 load tests |

The "next" line is honest: phases 6+ are not started. See [`PROJECT-STATUS.md`](PROJECT-STATUS.md) for a fuller breakdown of what's verified vs. unverified.

---

## Tech stack

- **Java 21** (records, sealed interfaces, pattern matching, virtual threads where useful)
- **Spring Boot 3.3.4** + **Spring Cloud 2023.0.3**
- **PostgreSQL 16**, **Apache Kafka 3.7**, **Redis 7**, **OpenSearch 2.x**
- **Resilience4j 2.2**, **Micrometer Tracing**, **Zipkin**
- **Flyway** for DB migrations, **hibernate-types** for JSONB columns
- **Avro** on the Kafka wire; **JSONB** in the event store
- **Spring Authorization Server** for OAuth2 / JWT
- **Razorpay Java SDK** (`com.razorpay:razorpay-java`) in test mode
- **Docker** (multi-stage, distroless base, non-root user)

See [`docs/INTERVIEW-CHEATSHEET.md`](docs/INTERVIEW-CHEATSHEET.md) for Q&A by topic.

---

## What this project is (and isn't)

**It is:** a structural reference. Every service follows Spring Boot conventions, has a designer-notes doc, and exercises a real microservice pattern. Good for interviews, good for onboarding, good as a starting point.

**It isn't (yet):** a running, end-to-end tested system. The code is consistent with itself, but **`mvn install` has never completed** on this machine (Maven 3.3.9 is too old for Spring Boot 3.3.4's plugin requirements — see `RUN-THE-BIBLE.md` §0.2). There are no integration tests. The Docker stack has never been brought up end-to-end. The first `mvn compile` will likely surface 5-30 small errors (mostly missing imports or SDK method names).

**Honest plan to get there:** `RUN-THE-BIBLE.md` is the step-by-step. After upgrading Maven and running the first compile, fix the topmost error, recompile, repeat. That loop converges in one focused session.

---

## License

This is an interview-prep project. Use it however you want.
